{{- if and .Values.media.enabled (not .Values.media.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "qbittorrent.mediaClaimName" . }}
  namespace: {{ include "qbittorrent.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "qbittorrent.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with (include "qbittorrent.mediaAnnotations" .) }}
  annotations: {{- . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.media.accessMode }}
  resources:
    requests:
      storage: {{ .Values.media.size }}
{{- end }}
