{{- if and .Values.config.enabled (not .Values.config.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "calibreweb.configClaimName" . }}
  namespace: {{ include "calibreweb.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "calibreweb.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with (include "calibreweb.configAnnotations" .) }}
  annotations: {{- . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.config.accessMode }}
  resources:
    requests:
      storage: {{ .Values.config.size }}
{{- end }}
