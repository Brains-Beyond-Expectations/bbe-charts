{{- if and .Values.media.enabled (not .Values.media.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "prowlarr.mediaClaimName" . }}
  namespace: {{ include "prowlarr.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "prowlarr.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with (include "prowlarr.mediaAnnotations" .) }}
  annotations: {{- . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.media.accessMode }}
  resources:
    requests:
      storage: {{ .Values.media.size }}
{{- end }}
