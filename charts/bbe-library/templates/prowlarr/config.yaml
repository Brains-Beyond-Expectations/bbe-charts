{{- $inBbeMedia := lookup "apps/v1" "Deployment" "bbe-media" "bbe-media-prowlarr" }}
{{- $standalone := lookup "apps/v1" "Deployment" "prowlarr" "prowlarr" }}
{{- if and (not (or $inBbeMedia $standalone)) .Values.prowlarrConfig.enabled (not .Values.prowlarrConfig.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "bbe-library.prowlarr.configClaimName" . }}
  namespace: {{ include "bbe-library.prowlarr.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "bbe-library.prowlarr.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with (include "bbe-library.prowlarr.configAnnotations" .) }}
  annotations: {{- . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.prowlarrConfig.accessMode }}
  resources:
    requests:
      storage: {{ .Values.prowlarrConfig.size }}
{{- end }}
