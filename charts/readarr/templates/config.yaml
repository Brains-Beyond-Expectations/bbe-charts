{{- if and .Values.config.enabled (not .Values.config.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "readarr.configClaimName" . }}
  namespace: {{ include "readarr.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "readarr.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with (include "readarr.configAnnotations" .) }}
  annotations: {{- . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.config.accessMode }}
  resources:
    requests:
      storage: {{ .Values.config.size }}
{{- end }}
